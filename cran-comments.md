## R CMD check results

0 errors | 0 warnings | 1 note

* checking installed package size ... NOTE
  Installed size is 5.3 MB; subdirectories `data` (2.9 MB) and `help` (1.6 MB)
  exceed 1 MB. The package includes two xz-compressed CASEN example datasets
  with realistic complex-survey structure and official labels. They are used by
  the executable examples and vignettes; version 0.3.5 adds only the household
  relationship variable required to demonstrate correct household-level income
  estimates.

## Test environments

* macOS aarch64 (Apple Silicon), R 4.4.3 (local),
  `R CMD check --as-cran --no-manual`.

## Downstream dependencies

There are currently no downstream dependencies for this package on CRAN.
