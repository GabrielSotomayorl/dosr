% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_bin.R
\name{multi_bin}
\alias{multi_bin}
\title{Perfilar Múltiples Variables Dicotómicas con Criterios de Calidad}
\usage{
multi_bin(
  design,
  vars_binarias,
  des = NULL,
  es_var_estudio = FALSE,
  filt = NULL,
  dir = "output",
  filename = NULL,
  decimales = 1,
  decimales_se = 3,
  verbose = TRUE
)
}
\arguments{
\item{design}{Un objeto `tbl_svy` de `srvyr`.}

\item{vars_binarias}{Un vector de strings con los nombres de las variables
dicotómicas (codificadas como 0/1) a perfilar.}

\item{des}{Un vector de strings con los nombres de las variables de
desagregación simple.}

\item{es_var_estudio}{Booleano. Si `TRUE`, aplica criterios de fiabilidad menos
estrictos para el tamaño muestral.}

\item{filt}{Un string con una expresión de filtro para `dplyr::filter()`.}

\item{dir}{Un string con la ruta del directorio de salida.}

\item{filename}{Un string con el nombre del archivo Excel.}

\item{decimales}{Entero. Número de decimales para la estimación puntual. Por defecto es 1.}

\item{decimales_se}{Entero. Número de decimales para el error estándar. Por defecto es 3.}

\item{verbose}{Booleano. Si `TRUE`, muestra mensajes de progreso.}
}
\value{
Un data.frame con todos los resultados consolidados (invisiblemente).
}
\description{
Para un **único diseño de encuesta**, calcula la proporción de
  "1s" para un vector de variables dicotómicas (0/1). Esta función es una
  herramienta de conveniencia para perfilados rápidos y análisis exploratorios.
  A diferencia de `obs_prop`, está optimizada para analizar múltiples
  variables dentro de una sola encuesta, pero no para comparar entre años.
  Genera un reporte en Excel con una hoja consolidada (con todas las métricas
  de calidad) y hojas de formato para el nivel nacional y cada desagregación
  simple.
}
